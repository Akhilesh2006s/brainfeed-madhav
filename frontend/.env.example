# API base URL.
# - In development (npm run dev), use .env.development and leave VITE_API_URL empty
#   so that Vite proxies /api to the local backend on :3001.
# - In production builds (npm run build), you can point this to your backend URL.
# For now we use localhost for everything.
VITE_API_URL=http://localhost:3001
